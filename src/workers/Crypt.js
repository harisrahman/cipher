function Crypt(text, encrypt)
{
	console.log(text, encrypt);

	// const result = "sx";
	// text.split('').forEach(letter =>
	// {
	// 	letter.charCodeAt(0);
	// 	String.fromCharCode(65);
	// });


	// return result;
	return "blah";
}

export default Crypt;
